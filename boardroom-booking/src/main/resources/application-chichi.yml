spring:
  application:
    name: Boardroom-Booking

  datasource:
    url: jdbc:postgresql://localhost:5432/boardroom
    username: postgres
    password: Chichi@dolls2024
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: Africa/Nairobi

logging:
  level:
    root: warn
    # org.springframework.boot: info



